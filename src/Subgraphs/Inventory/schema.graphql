schema {
  query: Query
  mutation: Mutation
}

"The node interface is implemented by entities that have a global unique identifier."
interface Node {
  id: ID!
}

type InventoryItem implements Node {
  id: ID!
  quantity: Int!
  product: Product! @cost(weight: "10")
}

type Mutation {
  restockProduct(input: RestockProductInput!): RestockProductPayload! @cost(weight: "10")
}

type Product {
  id: ID!
  stock: Int!
}

type Query {
  "Fetches an object given its ID."
  node("ID of the object." id: ID!): Node @cost(weight: "10")
  "Lookup nodes by a list of IDs."
  nodes("The list of node IDs." ids: [ID!]!): [Node]! @cost(weight: "10")
  inventoryVersion2: String! @cost(weight: "10")
  inventoryItemById(id: ID!): InventoryItem @lookup @cost(weight: "10")
  productById(id: ID!): Product @lookup @cost(weight: "10")
  productsById(ids: [ID!]!): [Product!] @cost(weight: "10")
}

type RestockProductPayload {
  product: Product
}

input RestockProductInput {
  id: ID!
  quantity: Int!
}

"The purpose of the `cost` directive is to define a `weight` for GraphQL types, fields, and arguments. Static analysis can use these weights when calculating the overall cost of a query or response."
directive @cost("The `weight` argument defines what value to add to the overall cost for every appearance, or possible appearance, of a type, field, argument, etc." weight: String!) on SCALAR | OBJECT | FIELD_DEFINITION | ARGUMENT_DEFINITION | ENUM | INPUT_FIELD_DEFINITION

directive @lookup on FIELD_DEFINITION

"The `Upload` scalar type represents a file upload."
scalar Upload
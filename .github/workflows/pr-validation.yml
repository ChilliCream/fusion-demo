name: Build

on:
  pull_request:
    branches: [main]

jobs:
  backend:
    name: Build Backend
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up .NET 10 SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --no-restore -c Release

      - name: Export Source Schemas
        run: |
          for project in Accounts Cart Inventory Order Payments Products Reviews Shipping; do
            dotnet run --project "src/SourceSchemas/$project" --no-build -c Release -- schema export
          done

      - name: Validate GraphQL schema
        uses: ChilliCream/nitro-fusion-validate@v16.0.0-rc.1.14
        with:
          source-schema-files: |
            src/SourceSchemas/Accounts/schema.graphqls
            src/SourceSchemas/Cart/schema.graphqls
            src/SourceSchemas/Inventory/schema.graphqls
            src/SourceSchemas/Order/schema.graphqls
            src/SourceSchemas/Payments/schema.graphqls
            src/SourceSchemas/Products/schema.graphqls
            src/SourceSchemas/Reviews/schema.graphqls
            src/SourceSchemas/Shipping/schema.graphqls
          stage: dev
          api-id: ${{ secrets.NITRO_API_ID }}
          api-key: ${{ secrets.NITRO_API_KEY }}

      - name: Validate MCP documents
        uses: ChilliCream/nitro-mcp-validate@v16.0.0-rc.1.14
        with:
          mcp-feature-collection-id: ${{ secrets.NITRO_MCP_FEATURE_COLLECTION_ID }}
          prompt-pattern: src/Gateway/Mcp/Prompts/*/*.json
          tool-pattern: src/Gateway/Mcp/Tools/*/*.graphql
          stage: dev
          api-key: ${{ secrets.NITRO_API_KEY }}

      - name: Validate OpenAPI documents
        uses: ChilliCream/nitro-openapi-validate@v16.0.0-rc.1.14
        with:
          openapi-collection-id: ${{ secrets.NITRO_OPENAPI_COLLECTION_ID }}
          patterns: src/Gateway/OpenApi/*.graphql
          stage: dev
          api-key: ${{ secrets.NITRO_API_KEY }}

  frontend:
    name: Build Frontend
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: src/frontend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: yarn
          cache-dependency-path: src/frontend/yarn.lock

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build
        run: yarn build

      - name: Persist Relay operations
        run: yarn relay:persist
        env:
          VITE_ENABLE_PERSISTED_OPERATIONS: "true"

      - name: Validate Client
        uses: ChilliCream/nitro-client-validate@v16.0.0-rc.1.14
        with:
          client-id: ${{ secrets.CLIENT_ID }}
          operations-file: src/frontend/operations.json
          stage: dev
          api-key: ${{ secrets.NITRO_API_KEY }}
